-- Anti-duplicate system
if game:GetService("CoreGui"):FindFirstChild("XDragonHubGUI") then
    game:GetService("CoreGui"):FindFirstChild("XDragonHubGUI"):Destroy()
end

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local LocalPlayer = Players.LocalPlayer

-- ================= NETWORKING =================
local MiningRemotes = ReplicatedStorage:FindFirstChild("MiningRemotes")
local CrystalPickup = MiningRemotes and MiningRemotes:FindFirstChild("CrystalPickup")

-- ================= GUI SETUP =================
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "XDragonHubGUI"
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

local success, _ = pcall(function()
    if gethui then
        ScreenGui.Parent = gethui()
    else
        ScreenGui.Parent = game:GetService("CoreGui")
    end
end)
if not success then
    ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")
end

-- Main Frame
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 420, 0, 550)
MainFrame.Position = UDim2.new(0.5, -210, 0.5, -275)
MainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.ClipsDescendants = true
MainFrame.Parent = ScreenGui

local FrameCorner = Instance.new("UICorner")
FrameCorner.CornerRadius = UDim.new(0, 16)
FrameCorner.Parent = MainFrame

-- Gradient border
local BorderFrame = Instance.new("Frame")
BorderFrame.Size = UDim2.new(1, 4, 1, 4)
BorderFrame.Position = UDim2.new(0, -2, 0, -2)
BorderFrame.BackgroundColor3 = Color3.fromRGB(0, 200, 255)
BorderFrame.BorderSizePixel = 0
BorderFrame.ZIndex = 0
BorderFrame.Parent = MainFrame

local BorderCorner = Instance.new("UICorner")
BorderCorner.CornerRadius = UDim.new(0, 16)
BorderCorner.Parent = BorderFrame

local InnerFrame = Instance.new("Frame")
InnerFrame.Size = UDim2.new(1, -4, 1, -4)
InnerFrame.Position = UDim2.new(0, 2, 0, 2)
InnerFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
InnerFrame.BorderSizePixel = 0
InnerFrame.ZIndex = 1
InnerFrame.Parent = MainFrame

local InnerCorner = Instance.new("UICorner")
InnerCorner.CornerRadius = UDim.new(0, 14)
InnerCorner.Parent = InnerFrame

-- Title bar
local TitleFrame = Instance.new("Frame")
TitleFrame.Size = UDim2.new(1, 0, 0, 60)
TitleFrame.BackgroundColor3 = Color3.fromRGB(0, 150, 255)
TitleFrame.BorderSizePixel = 0
TitleFrame.ZIndex = 2
TitleFrame.Parent = InnerFrame

local TitleGradient = Instance.new("UIGradient")
TitleGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(0, 120, 255)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(0, 200, 255))
})
TitleGradient.Parent = TitleFrame

local TitleCorner = Instance.new("UICorner")
TitleCorner.CornerRadius = UDim.new(0, 14)
TitleCorner.Parent = TitleFrame

local TitleText = Instance.new("TextLabel")
TitleText.Size = UDim2.new(0.8, 0, 1, 0)
TitleText.Position = UDim2.new(0.05, 0, 0, 0)
TitleText.BackgroundTransparency = 1
TitleText.Text = "Xdragon.Hub - Crystal Collecting Simulator"
TitleText.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleText.TextSize = 20
TitleText.Font = Enum.Font.GothamBold
TitleText.TextXAlignment = Enum.TextXAlignment.Left
TitleText.TextWrapped = true
TitleText.ZIndex = 3
TitleText.Parent = TitleFrame

-- Minimize button
local MinimizeBtn = Instance.new("TextButton")
MinimizeBtn.Size = UDim2.new(0, 30, 0, 30)
MinimizeBtn.Position = UDim2.new(1, -35, 0.5, -15)
MinimizeBtn.BackgroundColor3 = Color3.fromRGB(255, 80, 80)
MinimizeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
MinimizeBtn.Text = "—"
MinimizeBtn.Font = Enum.Font.GothamBold
MinimizeBtn.TextSize = 20
MinimizeBtn.ZIndex = 3
MinimizeBtn.Parent = TitleFrame

local MinimizeCorner = Instance.new("UICorner")
MinimizeCorner.CornerRadius = UDim.new(0, 8)
MinimizeCorner.Parent = MinimizeBtn

-- Content container
local ContentContainer = Instance.new("ScrollingFrame")
ContentContainer.Size = UDim2.new(1, -20, 1, -70)
ContentContainer.Position = UDim2.new(0, 10, 0, 65)
ContentContainer.BackgroundTransparency = 1
ContentContainer.CanvasSize = UDim2.new(0, 0, 0, 600)
ContentContainer.ScrollBarThickness = 3
ContentContainer.ScrollBarImageColor3 = Color3.fromRGB(0, 150, 255)
ContentContainer.ZIndex = 2
ContentContainer.Parent = InnerFrame

-- ================= DRAG FUNCTIONALITY =================
local dragging = false
local dragInput = nil
local dragStart = nil
local startPos = nil

local function updateDrag(input)
    local delta = input.Position - dragStart
    MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
end

MainFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = MainFrame.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

MainFrame.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        updateDrag(input)
    end
end)

-- ================= CIRCLE LOGO =================
local CircleLogo = Instance.new("ImageButton")
CircleLogo.Name = "CircleLogo"
CircleLogo.Size = UDim2.new(0, 60, 0, 60)
CircleLogo.Position = UDim2.new(0.5, -30, 0.5, -30)
CircleLogo.BackgroundColor3 = Color3.fromRGB(0, 150, 255)
CircleLogo.Image = "rbxassetid://1511976415632031754"
CircleLogo.Visible = false
CircleLogo.ZIndex = 5
CircleLogo.Parent = ScreenGui

local CircleCorner = Instance.new("UICorner")
CircleCorner.CornerRadius = UDim.new(1, 0)
CircleCorner.Parent = CircleLogo

local CircleBorder = Instance.new("UIStroke")
CircleBorder.Color = Color3.fromRGB(0, 200, 255)
CircleBorder.Thickness = 2
CircleBorder.Parent = CircleLogo

-- Circle drag
local circleDragging = false
local circleDragInput = nil
local circleDragStart = nil
local circleStartPos = nil

local function updateCircleDrag(input)
    local delta = input.Position - circleDragStart
    CircleLogo.Position = UDim2.new(circleStartPos.X.Scale, circleStartPos.X.Offset + delta.X, circleStartPos.Y.Scale, circleStartPos.Y.Offset + delta.Y)
end

CircleLogo.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        circleDragging = true
        circleDragStart = input.Position
        circleStartPos = CircleLogo.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                circleDragging = false
            end
        end)
    end
end)

CircleLogo.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        circleDragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input == circleDragInput and circleDragging then
        updateCircleDrag(input)
    end
end)

-- ================= MINIMIZE LOGIC =================
local isMinimized = false

local function toggleMinimize()
    isMinimized = not isMinimized
    if isMinimized then
        MainFrame.Visible = false
        CircleLogo.Visible = true
    else
        MainFrame.Visible = true
        CircleLogo.Visible = false
    end
end
MinimizeBtn.MouseButton1Click:Connect(toggleMinimize)
CircleLogo.MouseButton1Click:Connect(function()
    if not circleDragging then
        toggleMinimize()
    end
end)

-- ================= DATA & VARIABLES =================
local autoFarm = false
local autoSell = false
local farmSpeed = 1
local sellSpeed = 2

-- Crystal data
local CrystalList = {
    {name = "acorn Crystal", damage = 20},
    {name = "rocks Crystal", damage = 39},
    {name = "mushroom Crystal", damage = 14},
    {name = "Diamond Crystal", damage = 50},
    {name = "Ruby Crystal", damage = 75},
    {name = "Emerald Crystal", damage = 100},
    {name = "Sapphire Crystal", damage = 150},
    {name = "Amethyst Crystal", damage = 200},
}

-- ================= UI BUTTON BUILDER =================
local function createButton(text, pos, size, color)
    local btn = Instance.new("TextButton")
    btn.Size = size or UDim2.new(0.48, 0, 0, 90)
    btn.Position = pos
    btn.BackgroundColor3 = color or Color3.fromRGB(0, 180, 255)
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Text = text
    btn.Font = Enum.Font.GothamSemibold
    btn.TextSize = 18
    btn.ZIndex = 2
    btn.Parent = ContentContainer
    
    local btnCorner = Instance.new("UICorner")
    btnCorner.CornerRadius = UDim.new(0, 10)
    btnCorner.Parent = btn
    
    local btnGradient = Instance.new("UIGradient")
    btnGradient.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, btn.BackgroundColor3),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(0, 120, 200))
    })
    btnGradient.Parent = btn
    
    return btn
end

-- Buttons
local autoFarmBtn = createButton("Auto Farm:\n[Off]", UDim2.new(0, 0, 0, 0))
local autoSellBtn = createButton("Auto Sell:\n[Off]", UDim2.new(0.52, 0, 0, 0))

-- Speed controls
local FarmSpeedFrame = Instance.new("Frame")
FarmSpeedFrame.Size = UDim2.new(1, 0, 0, 60)
FarmSpeedFrame.Position = UDim2.new(0, 0, 0, 100)
FarmSpeedFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 40)
FarmSpeedFrame.ZIndex = 2
FarmSpeedFrame.Parent = ContentContainer

local FarmSpeedCorner = Instance.new("UICorner")
FarmSpeedCorner.CornerRadius = UDim.new(0, 10)
FarmSpeedCorner.Parent = FarmSpeedFrame

local FarmSpeedLabel = Instance.new("TextLabel")
FarmSpeedLabel.Size = UDim2.new(1, 0, 0, 20)
FarmSpeedLabel.Position = UDim2.new(0, 0, 0, 5)
FarmSpeedLabel.BackgroundTransparency = 1
FarmSpeedLabel.Text = "Farm Speed: 1 second"
FarmSpeedLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
FarmSpeedLabel.Font = Enum.Font.GothamSemibold
FarmSpeedLabel.TextSize = 14
FarmSpeedLabel.ZIndex = 3
FarmSpeedLabel.Parent = FarmSpeedFrame

local FarmSpeedInput = Instance.new("TextBox")
FarmSpeedInput.Size = UDim2.new(0.8, 0, 0, 25)
FarmSpeedInput.Position = UDim2.new(0.1, 0, 0, 30)
FarmSpeedInput.BackgroundColor3 = Color3.fromRGB(50, 50, 55)
FarmSpeedInput.TextColor3 = Color3.fromRGB(255, 255, 255)
FarmSpeedInput.Text = "1"
FarmSpeedInput.Font = Enum.Font.Gotham
FarmSpeedInput.TextSize = 14
FarmSpeedInput.ZIndex = 3
FarmSpeedInput.Parent = FarmSpeedFrame

local FarmSpeedCorner2 = Instance.new("UICorner")
FarmSpeedCorner2.CornerRadius = UDim.new(0, 5)
FarmSpeedCorner2.Parent = FarmSpeedInput

FarmSpeedInput.FocusLost:Connect(function(enterPressed)
    local newSpeed = tonumber(FarmSpeedInput.Text)
    if newSpeed and newSpeed >= 0.1 and newSpeed <= 10 then
        farmSpeed = newSpeed
        FarmSpeedLabel.Text = "Farm Speed: " .. tostring(newSpeed) .. " second(s)"
    else
        FarmSpeedInput.Text = tostring(farmSpeed)
    end
end)

-- ================= AUTO FARM FUNCTION =================
local function farmCrystal()
    if not CrystalPickup then return end
    
    local randomCrystal = CrystalList[math.random(1, #CrystalList)]
    
    pcall(function()
        firesignal(CrystalPickup.OnClientEvent, {
            ["success"] = true,
            ["combo"] = false,
            ["broken"] = false,
            ["crystalName"] = randomCrystal.name,
            ["amount"] = 1,
            ["damage"] = randomCrystal.damage,
        })
    end)
end

-- ================= AUTO SELL FUNCTION =================
local function findSellPrompt()
    local character = LocalPlayer.Character
    if not character then return nil end
    
    local root = character:FindFirstChild("HumanoidRootPart")
    if not root then return nil end
    
    -- Find sell prompts
    for _, obj in pairs(workspace:GetDescendants()) do
        if obj:IsA("ProximityPrompt") then
            local actionText = (obj.ActionText .. " " .. obj.ObjectText):lower()
            if actionText:find("sell") or actionText:find("trade") or actionText:find("exchange") or actionText:find("cash") then
                local distance = (root.Position - obj.Parent.Position).Magnitude
                if distance < 20 then
                    return obj
                end
            end
        end
    end
    
    -- Also check for sell parts
    for _, obj in pairs(workspace:GetDescendants()) do
        if obj:IsA("Part") or obj:IsA("Model") then
            local name = obj.Name:lower()
            if name:find("sell") or name:find("trade") or name:find("shop") or name:find("merchant") then
                local prompt = obj:FindFirstChildOfClass("ProximityPrompt")
                if prompt then
                    return prompt
                end
            end
        end
    end
    
    return nil
end

local function autoSellCrystals()
    local sellPrompt = findSellPrompt()
    if sellPrompt then
        pcall(function()
            fireproximityprompt(sellPrompt)
        end)
        return true
    end
    return false
end

-- ================= TOGGLE MECHANICS =================

autoFarmBtn.MouseButton1Click:Connect(function()
    autoFarm = not autoFarm
    autoFarmBtn.Text = "Auto Farm:\n" .. (autoFarm and "[On]" or "[Off]")
end)

autoSellBtn.MouseButton1Click:Connect(function()
    autoSell = not autoSell
    autoSellBtn.Text = "Auto Sell:\n" .. (autoSell and "[On]" or "[Off]")
end)

-- ================= MAIN LOOPS =================
-- Auto Farm Loop
task.spawn(function()
    while true do
        if autoFarm then
            farmCrystal()
        end
        task.wait(farmSpeed)
    end
end)

-- Auto Sell Loop
task.spawn(function()
    while true do
        if autoSell then
            autoSellCrystals()
        end
        task.wait(sellSpeed)
    end
end)

-- ================= STATUS DISPLAY =================
local StatusLabel = Instance.new("TextLabel")
StatusLabel.Size = UDim2.new(1, 0, 0, 60)
StatusLabel.Position = UDim2.new(0, 0, 0, 170)
StatusLabel.BackgroundColor3 = Color3.fromRGB(35, 35, 40)
StatusLabel.TextColor3 = Color3.fromRGB(0, 255, 0)
StatusLabel.Text = "Status: Ready"
StatusLabel.Font = Enum.Font.GothamSemibold
StatusLabel.TextSize = 16
StatusLabel.ZIndex = 2
StatusLabel.Parent = ContentContainer

local StatusCorner = Instance.new("UICorner")
StatusCorner.CornerRadius = UDim.new(0, 10)
StatusCorner.Parent = StatusLabel

-- Update status
task.spawn(function()
    while true do
        local status = "Status: "
        if autoFarm and autoSell then
            status = status .. "Auto Farm + Auto Sell Active"
            StatusLabel.TextColor3 = Color3.fromRGB(0, 255, 0)
        elseif autoFarm then
            status = status .. "Auto Farm Active"
            StatusLabel.TextColor3 = Color3.fromRGB(0, 255, 0)
        elseif autoSell then
            status = status .. "Auto Sell Active"
            StatusLabel.TextColor3 = Color3.fromRGB(0, 255, 0)
        else
            status = status .. "Ready"
            StatusLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
        end
        StatusLabel.Text = status
        task.wait(0.5)
    end
end)

-- ================= INFORMATION =================
local InfoLabel = Instance.new("TextLabel")
InfoLabel.Size = UDim2.new(1, 0, 0, 100)
InfoLabel.Position = UDim2.new(0, 0, 0, 240)
InfoLabel.BackgroundTransparency = 1
InfoLabel.Text = "Xdragon.Hub\nCrystal Collecting Simulator\nAuto Farm & Auto Sell Script"
InfoLabel.TextColor3 = Color3.fromRGB(150, 150, 150)
InfoLabel.TextSize = 14
InfoLabel.Font = Enum.Font.GothamSemibold
InfoLabel.TextWrapped = true
InfoLabel.ZIndex = 2
InfoLabel.Parent = ContentContainer
