-- ==================== XDRAGON HUB - DON'T CRASH BY TRAIN ====================
-- Anti-duplicate system
if game:GetService("CoreGui"):FindFirstChild("XDragonHub") then
    game:GetService("CoreGui"):FindFirstChild("XDragonHub"):Destroy()
end

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local LocalPlayer = Players.LocalPlayer

-- ==================== GUI SETUP ====================
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "XDragonHub"
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

local success, _ = pcall(function()
    if gethui then
        ScreenGui.Parent = gethui()
    else
        ScreenGui.Parent = game:GetService("CoreGui")
    end
end)
if not success then
    ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")
end

-- ==================== MAIN FRAME ====================
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 420, 0, 600)
MainFrame.Position = UDim2.new(0.5, -210, 0.5, -300)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.ClipsDescendants = true
MainFrame.Parent = ScreenGui

local FrameCorner = Instance.new("UICorner")
FrameCorner.CornerRadius = UDim.new(0, 16)
FrameCorner.Parent = MainFrame

-- Gradient Border
local BorderFrame = Instance.new("Frame")
BorderFrame.Size = UDim2.new(1, 4, 1, 4)
BorderFrame.Position = UDim2.new(0, -2, 0, -2)
BorderFrame.BackgroundColor3 = Color3.fromRGB(255, 100, 0)
BorderFrame.BorderSizePixel = 0
BorderFrame.ZIndex = 0
BorderFrame.Parent = MainFrame

local BorderGradient = Instance.new("UIGradient")
BorderGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 100, 0)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(255, 200, 0)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 100, 0))
})
BorderGradient.Rotation = 45
BorderGradient.Parent = BorderFrame

local BorderCorner = Instance.new("UICorner")
BorderCorner.CornerRadius = UDim.new(0, 16)
BorderCorner.Parent = BorderFrame

local InnerFrame = Instance.new("Frame")
InnerFrame.Size = UDim2.new(1, -4, 1, -4)
InnerFrame.Position = UDim2.new(0, 2, 0, 2)
InnerFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
InnerFrame.BorderSizePixel = 0
InnerFrame.ZIndex = 1
InnerFrame.Parent = MainFrame

local InnerCorner = Instance.new("UICorner")
InnerCorner.CornerRadius = UDim.new(0, 14)
InnerCorner.Parent = InnerFrame

-- ==================== HEADER ====================
local Header = Instance.new("Frame")
Header.Size = UDim2.new(1, 0, 0, 60)
Header.BackgroundColor3 = Color3.fromRGB(255, 100, 0)
Header.BorderSizePixel = 0
Header.ZIndex = 2
Header.Parent = InnerFrame

local HeaderGradient = Instance.new("UIGradient")
HeaderGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 100, 0)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 150, 50))
})
HeaderGradient.Parent = Header

local HeaderCorner = Instance.new("UICorner")
HeaderCorner.CornerRadius = UDim.new(0, 14)
HeaderCorner.Parent = Header

-- Logo in Header
local HeaderLogo = Instance.new("ImageLabel")
HeaderLogo.Size = UDim2.new(0, 40, 0, 40)
HeaderLogo.Position = UDim2.new(0, 10, 0, 10)
HeaderLogo.BackgroundTransparency = 1
HeaderLogo.Image = "rbxassetid://116012973251361"
HeaderLogo.ZIndex = 3
HeaderLogo.Parent = Header

local HeaderLogoCorner = Instance.new("UICorner")
HeaderLogoCorner.CornerRadius = UDim.new(1, 0)
HeaderLogoCorner.Parent = HeaderLogo

local TitleText = Instance.new("TextLabel")
TitleText.Size = UDim2.new(0.6, 0, 1, 0)
TitleText.Position = UDim2.new(0, 60, 0, 0)
TitleText.BackgroundTransparency = 1
TitleText.Text = "🚂 XDRAGON HUB"
TitleText.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleText.TextSize = 18
TitleText.Font = Enum.Font.GothamBlack
TitleText.TextXAlignment = Enum.TextXAlignment.Left
TitleText.ZIndex = 3
TitleText.Parent = Header

local MinimizeBtn = Instance.new("TextButton")
MinimizeBtn.Size = UDim2.new(0, 30, 0, 30)
MinimizeBtn.Position = UDim2.new(1, -40, 0.5, -15)
MinimizeBtn.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
MinimizeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
MinimizeBtn.Text = "—"
MinimizeBtn.Font = Enum.Font.GothamBold
MinimizeBtn.TextSize = 20
MinimizeBtn.ZIndex = 3
MinimizeBtn.Parent = Header

local MinimizeCorner = Instance.new("UICorner")
MinimizeCorner.CornerRadius = UDim.new(0, 8)
MinimizeCorner.Parent = MinimizeBtn

-- ==================== CONTENT CONTAINER ====================
local ContentContainer = Instance.new("ScrollingFrame")
ContentContainer.Size = UDim2.new(1, -20, 1, -70)
ContentContainer.Position = UDim2.new(0, 10, 0, 65)
ContentContainer.BackgroundTransparency = 1
ContentContainer.CanvasSize = UDim2.new(0, 0, 0, 500)
ContentContainer.ScrollBarThickness = 4
ContentContainer.ScrollBarImageColor3 = Color3.fromRGB(255, 100, 0)
ContentContainer.ZIndex = 2
ContentContainer.Parent = InnerFrame

-- ==================== VARIABLES ====================
local flyOn = false
local autoBuyOn = false
local autoBuySpeedOn = false
local flyBodyVel = nil
local flyBodyGyro = nil
local flySpeed = 50
local pcsAmount = 1
local selectedArea = "Common Area"
local selectedMutation = "Normal1"
local selectedAutoBuyItems = {}

-- ==================== DATA ====================
local Areas = {
    "Common Area",
    "Rare Area",
    "Legendary Area",
    "Mythic Area",
    "Godly Area"
}

local Mutations = {
    "Normal1",
    "Gold1",
    "Diamond1",
    "Crab1",
    "Alien1"
}

local AutoBuyItems = {
    "Chimpanzini Bananini",
    "Trippi Troppi Troppa",
    "Pot Hotspot",
    "Job Job Job Sahur",
    "Boneca Ambalabu",
    "Esok Sekulah",
    "Karkerkar Kurkur"
}

local AdminCategories = {
    {Name = "Common", Items = {"Bananita Dolphinita", "Trippi Troppi", "Gangster Footera"}},
    {Name = "Rare", Items = {"Ballerina Cappuccina", "Svinina Boombardino", "Bombombini Gusini"}},
    {Name = "Legendary", Items = {"Cappuccino Assassino", "La Vacca Saturno"}},
    {Name = "Mythic", Items = {"Crab Chef", "Matteo", "Tralalero Tralala"}},
    {Name = "Godly", Items = {"Giraffa Celestre", "Six Seven", "Odin Din Din Dun"}},
    {Name = "Limited", Items = {"Strawberry Elephant", "Summerini Fattini"}}
}

-- Default all auto buy items selected
for _, item in ipairs(AutoBuyItems) do
    selectedAutoBuyItems[item] = true
end

-- ==================== UI BUTTON BUILDER ====================
local function createButton(text, pos, size, color)
    local btn = Instance.new("TextButton")
    btn.Size = size or UDim2.new(0.48, 0, 0, 50)
    btn.Position = pos
    btn.BackgroundColor3 = color or Color3.fromRGB(255, 100, 0)
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Text = text
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 14
    btn.ZIndex = 2
    btn.Parent = ContentContainer
    
    local btnCorner = Instance.new("UICorner")
    btnCorner.CornerRadius = UDim.new(0, 8)
    btnCorner.Parent = btn
    
    return btn
end

-- ==================== MONEY GIVER ====================
local MoneyFrame = Instance.new("Frame")
MoneyFrame.Size = UDim2.new(1, 0, 0, 70)
MoneyFrame.Position = UDim2.new(0, 0, 0, 0)
MoneyFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
MoneyFrame.ZIndex = 2
MoneyFrame.Parent = ContentContainer

local MoneyCorner = Instance.new("UICorner")
MoneyCorner.CornerRadius = UDim.new(0, 10)
MoneyCorner.Parent = MoneyFrame

local MoneyLabel = Instance.new("TextLabel")
MoneyLabel.Size = UDim2.new(1, 0, 0, 20)
MoneyLabel.Position = UDim2.new(0, 0, 0, 5)
MoneyLabel.BackgroundTransparency = 1
MoneyLabel.Text = "💰 MONEY GIVER"
MoneyLabel.TextColor3 = Color3.fromRGB(255, 200, 0)
MoneyLabel.TextSize = 14
MoneyLabel.Font = Enum.Font.GothamBold
MoneyLabel.ZIndex = 3
MoneyLabel.Parent = MoneyFrame

local MoneyInput = Instance.new("TextBox")
MoneyInput.Size = UDim2.new(0.6, 0, 0, 30)
MoneyInput.Position = UDim2.new(0.05, 0, 0, 35)
MoneyInput.BackgroundColor3 = Color3.fromRGB(50, 50, 55)
MoneyInput.TextColor3 = Color3.fromRGB(255, 255, 255)
MoneyInput.PlaceholderText = "Amount (0-100T)"
MoneyInput.PlaceholderColor3 = Color3.fromRGB(150, 150, 150)
MoneyInput.Text = "1000000000000"
MoneyInput.Font = Enum.Font.Gotham
MoneyInput.TextSize = 12
MoneyInput.ZIndex = 3
MoneyInput.Parent = MoneyFrame

local MoneyInputCorner = Instance.new("UICorner")
MoneyInputCorner.CornerRadius = UDim.new(0, 5)
MoneyInputCorner.Parent = MoneyInput

local GiveMoneyBtn = Instance.new("TextButton")
GiveMoneyBtn.Size = UDim2.new(0.3, 0, 0, 30)
GiveMoneyBtn.Position = UDim2.new(0.68, 0, 0, 35)
GiveMoneyBtn.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
GiveMoneyBtn.TextColor3 = Color3.fromRGB(0, 0, 0)
GiveMoneyBtn.Text = "GIVE MONEY"
GiveMoneyBtn.Font = Enum.Font.GothamBold
GiveMoneyBtn.TextSize = 11
GiveMoneyBtn.ZIndex = 3
GiveMoneyBtn.Parent = MoneyFrame

local GiveMoneyCorner = Instance.new("UICorner")
GiveMoneyCorner.CornerRadius = UDim.new(0, 5)
GiveMoneyCorner.Parent = GiveMoneyBtn

GiveMoneyBtn.MouseButton1Click:Connect(function()
    local amount = tonumber(MoneyInput.Text)
    if amount and amount > 0 and amount <= 100000000000000 then
        local leaderstats = LocalPlayer:FindFirstChild("leaderstats")
        if leaderstats then
            local money = leaderstats:FindFirstChild("Money")
            if money then
                money.Value = amount
            end
        end
    end
end)

-- ==================== FLY BUTTON ====================
local flyBtn = createButton("🦅 Fly: [OFF]", UDim2.new(0, 0, 0, 80))

flyBtn.MouseButton1Click:Connect(function()
    flyOn = not flyOn
    flyBtn.Text = "🦅 Fly: [" .. (flyOn and "ON" or "OFF") .. "]"
    flyBtn.BackgroundColor3 = flyOn and Color3.fromRGB(0, 255, 0) or Color3.fromRGB(255, 100, 0)
    
    local char = LocalPlayer.Character
    if not char or not char:FindFirstChild("HumanoidRootPart") then return end
    local hrp = char.HumanoidRootPart
    
    if flyOn then
        flyBodyVel = Instance.new("BodyVelocity")
        flyBodyVel.MaxForce = Vector3.new(1e5, 1e5, 1e5)
        flyBodyVel.Velocity = Vector3.zero
        flyBodyVel.Parent = hrp
        
        flyBodyGyro = Instance.new("BodyGyro")
        flyBodyGyro.MaxTorque = Vector3.new(1e5, 1e5, 1e5)
        flyBodyGyro.CFrame = hrp.CFrame
        flyBodyGyro.Parent = hrp
    else
        if flyBodyVel then flyBodyVel:Destroy() end
        if flyBodyGyro then flyBodyGyro:Destroy() end
        flyBodyVel = nil
        flyBodyGyro = nil
    end
end)

-- ==================== AUTO BUY BUTTON (With Popup) ====================
local autoBuyBtn = createButton("🛒 Auto Buy: [OFF]", UDim2.new(0, 0, 0, 140), UDim2.new(0.48, 0, 0, 50))
local autoBuyItemsBtn = createButton("📋 Auto Buy Items", UDim2.new(0.52, 0, 0, 140), UDim2.new(0.48, 0, 0, 50), Color3.fromRGB(0, 150, 255))

autoBuyBtn.MouseButton1Click:Connect(function()
    autoBuyOn = not autoBuyOn
    autoBuyBtn.Text = "🛒 Auto Buy: [" .. (autoBuyOn and "ON" or "OFF") .. "]"
    autoBuyBtn.BackgroundColor3 = autoBuyOn and Color3.fromRGB(0, 255, 0) or Color3.fromRGB(255, 100, 0)
end)

-- Auto Buy Items Popup
local AutoBuyPopup = Instance.new("Frame")
AutoBuyPopup.Size = UDim2.new(0, 380, 0, 350)
AutoBuyPopup.Position = UDim2.new(0.5, -190, 0.5, -175)
AutoBuyPopup.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
AutoBuyPopup.Visible = false
AutoBuyPopup.ZIndex = 10
AutoBuyPopup.Parent = ScreenGui

local AutoBuyPopupCorner = Instance.new("UICorner")
AutoBuyPopupCorner.CornerRadius = UDim.new(0, 12)
AutoBuyPopupCorner.Parent = AutoBuyPopup

local AutoBuyPopupTitle = Instance.new("TextLabel")
AutoBuyPopupTitle.Size = UDim2.new(1, 0, 0, 40)
AutoBuyPopupTitle.BackgroundColor3 = Color3.fromRGB(0, 150, 255)
AutoBuyPopupTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
AutoBuyPopupTitle.Text = "🛒 SELECT AUTO BUY ITEMS"
AutoBuyPopupTitle.Font = Enum.Font.GothamBold
AutoBuyPopupTitle.TextSize = 16
AutoBuyPopupTitle.ZIndex = 11
AutoBuyPopupTitle.Parent = AutoBuyPopup

local AutoBuyPopupTitleCorner = Instance.new("UICorner")
AutoBuyPopupTitleCorner.CornerRadius = UDim.new(0, 12)
AutoBuyPopupTitleCorner.Parent = AutoBuyPopupTitle

local CloseAutoBuyPopup = Instance.new("TextButton")
CloseAutoBuyPopup.Size = UDim2.new(0, 30, 0, 30)
CloseAutoBuyPopup.Position = UDim2.new(1, -35, 0, 5)
CloseAutoBuyPopup.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
CloseAutoBuyPopup.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseAutoBuyPopup.Text = "X"
CloseAutoBuyPopup.Font = Enum.Font.GothamBold
CloseAutoBuyPopup.TextSize = 18
CloseAutoBuyPopup.ZIndex = 11
CloseAutoBuyPopup.Parent = AutoBuyPopup

local CloseAutoBuyCorner = Instance.new("UICorner")
CloseAutoBuyCorner.CornerRadius = UDim.new(0, 6)
CloseAutoBuyCorner.Parent = CloseAutoBuyPopup

local AutoBuyScroll = Instance.new("ScrollingFrame")
AutoBuyScroll.Size = UDim2.new(1, -20, 1, -50)
AutoBuyScroll.Position = UDim2.new(0, 10, 0, 45)
AutoBuyScroll.BackgroundTransparency = 1
AutoBuyScroll.CanvasSize = UDim2.new(0, 0, 0, #AutoBuyItems * 40)
AutoBuyScroll.ScrollBarThickness = 3
AutoBuyScroll.ZIndex = 11
AutoBuyScroll.Parent = AutoBuyPopup

local AutoBuyLayout = Instance.new("UIListLayout")
AutoBuyLayout.Padding = UDim.new(0, 5)
AutoBuyLayout.Parent = AutoBuyScroll

autoBuyItemsBtn.MouseButton1Click:Connect(function()
    AutoBuyPopup.Visible = true
end)

CloseAutoBuyPopup.MouseButton1Click:Connect(function()
    AutoBuyPopup.Visible = false
end)

-- Create Auto Buy item buttons in popup
for _, item in ipairs(AutoBuyItems) do
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, 0, 0, 35)
    btn.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
    btn.TextColor3 = Color3.fromRGB(0, 0, 0)
    btn.Text = "✅ " .. item
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 13
    btn.ZIndex = 11
    btn.Parent = AutoBuyScroll
    
    local btnCorner = Instance.new("UICorner")
    btnCorner.CornerRadius = UDim.new(0, 5)
    btnCorner.Parent = btn
    
    btn.MouseButton1Click:Connect(function()
        selectedAutoBuyItems[item] = not selectedAutoBuyItems[item]
        if selectedAutoBuyItems[item] then
            btn.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
            btn.TextColor3 = Color3.fromRGB(0, 0, 0)
            btn.Text = "✅ " .. item
        else
            btn.BackgroundColor3 = Color3.fromRGB(50, 50, 55)
            btn.TextColor3 = Color3.fromRGB(255, 255, 255)
            btn.Text = "❌ " .. item
        end
    end)
end

-- ==================== AUTO BUY SPEED ====================
local autoBuySpeedBtn = createButton("⚡ AutoBuySpeed", UDim2.new(0, 0, 0, 200), UDim2.new(0.48, 0, 0, 50), Color3.fromRGB(255, 150, 0))

autoBuySpeedBtn.MouseButton1Click:Connect(function()
    autoBuySpeedOn = not autoBuySpeedOn
    autoBuySpeedBtn.Text = "⚡ AutoBuySpeed: [" .. (autoBuySpeedOn and "ON" or "OFF") .. "]"
    autoBuySpeedBtn.BackgroundColor3 = autoBuySpeedOn and Color3.fromRGB(0, 255, 0) or Color3.fromRGB(255, 150, 0)
end)

-- ==================== ADMIN SPAWN BUTTON (With Popup) ====================
local adminSpawnBtn = createButton("👑 Admin Spawn", UDim2.new(0, 0, 0, 260), UDim2.new(1, 0, 0, 50), Color3.fromRGB(255, 200, 0))

local AdminPopup = Instance.new("Frame")
AdminPopup.Size = UDim2.new(0, 380, 0, 450)
AdminPopup.Position = UDim2.new(0.5, -190, 0.5, -225)
AdminPopup.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
AdminPopup.Visible = false
AdminPopup.ZIndex = 10
AdminPopup.Parent = ScreenGui

local AdminPopupCorner = Instance.new("UICorner")
AdminPopupCorner.CornerRadius = UDim.new(0, 12)
AdminPopupCorner.Parent = AdminPopup

local AdminPopupTitle = Instance.new("TextLabel")
AdminPopupTitle.Size = UDim2.new(1, 0, 0, 40)
AdminPopupTitle.BackgroundColor3 = Color3.fromRGB(255, 200, 0)
AdminPopupTitle.TextColor3 = Color3.fromRGB(0, 0, 0)
AdminPopupTitle.Text = "👑 ADMIN SPAWN"
AdminPopupTitle.Font = Enum.Font.GothamBold
AdminPopupTitle.TextSize = 16
AdminPopupTitle.ZIndex = 11
AdminPopupTitle.Parent = AdminPopup

local AdminPopupTitleCorner = Instance.new("UICorner")
AdminPopupTitleCorner.CornerRadius = UDim.new(0, 12)
AdminPopupTitleCorner.Parent = AdminPopupTitle

local CloseAdminPopup = Instance.new("TextButton")
CloseAdminPopup.Size = UDim2.new(0, 30, 0, 30)
CloseAdminPopup.Position = UDim2.new(1, -35, 0, 5)
CloseAdminPopup.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
CloseAdminPopup.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseAdminPopup.Text = "X"
CloseAdminPopup.Font = Enum.Font.GothamBold
CloseAdminPopup.TextSize = 18
CloseAdminPopup.ZIndex = 11
CloseAdminPopup.Parent = AdminPopup

local CloseAdminCorner = Instance.new("UICorner")
CloseAdminCorner.CornerRadius = UDim.new(0, 6)
CloseAdminCorner.Parent = CloseAdminPopup

local AdminScroll = Instance.new("ScrollingFrame")
AdminScroll.Size = UDim2.new(1, -20, 1, -110)
AdminScroll.Position = UDim2.new(0, 10, 0, 100)
AdminScroll.BackgroundTransparency = 1
AdminScroll.CanvasSize = UDim2.new(0, 0, 0, 800)
AdminScroll.ScrollBarThickness = 3
AdminScroll.ZIndex = 11
AdminScroll.Parent = AdminPopup

local AdminLayout = Instance.new("UIListLayout")
AdminLayout.Padding = UDim.new(0, 5)
AdminLayout.Parent = AdminScroll

adminSpawnBtn.MouseButton1Click:Connect(function()
    AdminPopup.Visible = true
end)

CloseAdminPopup.MouseButton1Click:Connect(function()
    AdminPopup.Visible = false
end)

-- Controls inside Admin Popup
local PcsLabel = Instance.new("TextLabel")
PcsLabel.Size = UDim2.new(0.3, 0, 0, 30)
PcsLabel.Position = UDim2.new(0.05, 0, 0, 45)
PcsLabel.BackgroundTransparency = 1
PcsLabel.Text = "📦 PCS:"
PcsLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
PcsLabel.TextSize = 14
PcsLabel.Font = Enum.Font.GothamBold
PcsLabel.ZIndex = 11
PcsLabel.Parent = AdminPopup

local PcsInput = Instance.new("TextBox")
PcsInput.Size = UDim2.new(0.6, 0, 0, 30)
PcsInput.Position = UDim2.new(0.35, 0, 0, 45)
PcsInput.BackgroundColor3 = Color3.fromRGB(50, 50, 55)
PcsInput.TextColor3 = Color3.fromRGB(255, 255, 255)
PcsInput.Text = "1"
PcsInput.Font = Enum.Font.Gotham
PcsInput.TextSize = 14
PcsInput.ZIndex = 11
PcsInput.Parent = AdminPopup

local PcsCorner = Instance.new("UICorner")
PcsCorner.CornerRadius = UDim.new(0, 5)
PcsCorner.Parent = PcsInput

PcsInput.FocusLost:Connect(function()
    local val = tonumber(PcsInput.Text)
    if val and val > 0 then
        pcsAmount = val
    else
        PcsInput.Text = tostring(pcsAmount)
    end
end)

local AreaDropdown = Instance.new("TextButton")
AreaDropdown.Size = UDim2.new(0.45, 0, 0, 30)
AreaDropdown.Position = UDim2.new(0.05, 0, 0, 80)
AreaDropdown.BackgroundColor3 = Color3.fromRGB(50, 50, 55)
AreaDropdown.TextColor3 = Color3.fromRGB(255, 255, 255)
AreaDropdown.Text = "📍 " .. selectedArea
AreaDropdown.Font = Enum.Font.GothamBold
AreaDropdown.TextSize = 11
AreaDropdown.ZIndex = 11
AreaDropdown.Parent = AdminPopup

local AreaCorner = Instance.new("UICorner")
AreaCorner.CornerRadius = UDim.new(0, 5)
AreaCorner.Parent = AreaDropdown

AreaDropdown.MouseButton1Click:Connect(function()
    local idx = table.find(Areas, selectedArea) or 1
    idx = idx + 1
    if idx > #Areas then idx = 1 end
    selectedArea = Areas[idx]
    AreaDropdown.Text = "📍 " .. selectedArea
end)

local MutationDropdown = Instance.new("TextButton")
MutationDropdown.Size = UDim2.new(0.45, 0, 0, 30)
MutationDropdown.Position = UDim2.new(0.52, 0, 0, 80)
MutationDropdown.BackgroundColor3 = Color3.fromRGB(50, 50, 55)
MutationDropdown.TextColor3 = Color3.fromRGB(255, 255, 255)
MutationDropdown.Text = "🧬 " .. selectedMutation
MutationDropdown.Font = Enum.Font.GothamBold
MutationDropdown.TextSize = 11
MutationDropdown.ZIndex = 11
MutationDropdown.Parent = AdminPopup

local MutationCorner = Instance.new("UICorner")
MutationCorner.CornerRadius = UDim.new(0, 5)
MutationCorner.Parent = MutationDropdown

MutationDropdown.MouseButton1Click:Connect(function()
    local idx = table.find(Mutations, selectedMutation) or 1
    idx = idx + 1
    if idx > #Mutations then idx = 1 end
    selectedMutation = Mutations[idx]
    MutationDropdown.Text = "🧬 " .. selectedMutation
end)

-- Create Admin Spawn buttons in popup
for _, category in ipairs(AdminCategories) do
    local catLabel = Instance.new("TextLabel")
    catLabel.Size = UDim2.new(1, 0, 0, 25)
    catLabel.BackgroundColor3 = Color3.fromRGB(255, 100, 0)
    catLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    catLabel.Text = "  " .. category.Name:upper()
    catLabel.Font = Enum.Font.GothamBlack
    catLabel.TextSize = 13
    catLabel.TextXAlignment = Enum.TextXAlignment.Left
    catLabel.ZIndex = 11
    catLabel.Parent = AdminScroll
    
    local catCorner = Instance.new("UICorner")
    catCorner.CornerRadius = UDim.new(0, 5)
    catCorner.Parent = catLabel
    
    for _, item in ipairs(category.Items) do
        local btn = Instance.new("TextButton")
        btn.Size = UDim2.new(1, 0, 0, 30)
        btn.BackgroundColor3 = Color3.fromRGB(50, 50, 55)
        btn.TextColor3 = Color3.fromRGB(255, 255, 255)
        btn.Text = item
        btn.Font = Enum.Font.Gotham
        btn.TextSize = 12
        btn.ZIndex = 11
        btn.Parent = AdminScroll
        
        local btnCorner = Instance.new("UICorner")
        btnCorner.CornerRadius = UDim.new(0, 5)
        btnCorner.Parent = btn
        
        btn.MouseButton1Click:Connect(function()
            pcall(function()
                ReplicatedStorage.AdminSpawnEvent:FireServer(table.unpack({
                    [1] = item,
                    [2] = pcsAmount,
                    [3] = selectedArea,
                    [4] = true,
                    [5] = selectedMutation,
                }))
            end)
        end)
    end
end

-- ==================== MAIN LOOPS ====================
-- Fly loop
RunService.RenderStepped:Connect(function()
    if flyOn and flyBodyVel and flyBodyGyro then
        local char = LocalPlayer.Character
        if char and char:FindFirstChild("HumanoidRootPart") then
            local hrp = char.HumanoidRootPart
            local hum = char:FindFirstChildOfClass("Humanoid")
            
            if hum then
                local cam = workspace.CurrentCamera
                local moveDir = hum.MoveDirection
                
                if moveDir.Magnitude > 0 then
                    flyBodyVel.Velocity = (cam.CFrame.LookVector * moveDir.Z + cam.CFrame.RightVector * moveDir.X) * flySpeed
                else
                    flyBodyVel.Velocity = Vector3.zero
                end
                
                flyBodyGyro.CFrame = cam.CFrame
            end
        end
    end
end)

-- Auto Buy loop (only buys selected items)
task.spawn(function()
    while true do
        if autoBuyOn then
            for item, isSelected in pairs(selectedAutoBuyItems) do
                if isSelected then
                    pcall(function()
                        ReplicatedStorage.ShopEvent:FireServer(table.unpack({
                            [1] = "Buy",
                            [2] = item,
                        }))
                    end)
                    task.wait(0.1)
                end
            end
        end
        task.wait(0.5)
    end
end)

-- Auto Buy Speed loop
task.spawn(function()
    while true do
        if autoBuySpeedOn then
            pcall(function()
                ReplicatedStorage.BuySpeedEvent:FireServer(10)
            end)
        end
        task.wait(0.5)
    end
end)

-- ==================== MINIMIZE WITH DRAGGABLE LOGO ====================
local isMinimized = false

local CircleLogo = Instance.new("ImageButton")
CircleLogo.Size = UDim2.new(0, 60, 0, 60)
CircleLogo.Position = UDim2.new(0.5, -30, 0.5, -30)
CircleLogo.BackgroundTransparency = 1
CircleLogo.Image = "rbxassetid://116012973251361"
CircleLogo.Visible = false
CircleLogo.ZIndex = 10
CircleLogo.Parent = ScreenGui

MinimizeBtn.MouseButton1Click:Connect(function()
    isMinimized = not isMinimized
    MainFrame.Visible = not isMinimized
    CircleLogo.Visible = isMinimized
end)

-- Circle Logo Drag
local circleDragging = false
local circleDragInput = nil
local circleDragStart = nil
local circleStartPos = nil

local function updateCircleDrag(input)
    local delta = input.Position - circleDragStart
    CircleLogo.Position = UDim2.new(circleStartPos.X.Scale, circleStartPos.X.Offset + delta.X, circleStartPos.Y.Scale, circleStartPos.Y.Offset + delta.Y)
end

CircleLogo.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        circleDragging = true
        circleDragStart = input.Position
        circleStartPos = CircleLogo.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                circleDragging = false
            end
        end)
    end
end)

CircleLogo.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        circleDragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input == circleDragInput and circleDragging then
        updateCircleDrag(input)
    end
end)

CircleLogo.MouseButton1Click:Connect(function()
    if not circleDragging then
        isMinimized = false
        MainFrame.Visible = true
        CircleLogo.Visible = false
    end
end)

-- ==================== DRAG FUNCTIONALITY ====================
local dragging = false
local dragInput = nil
local dragStart = nil
local startPos = nil

local function updateDrag(input)
    local delta = input.Position - dragStart
    MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
end

MainFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = MainFrame.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

MainFrame.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        updateDrag(input)
    end
end)

print("🚂 Xdragon Hub - Don't Crash By Train loaded!")
print("💎 Logo: 116012973251361")
print("🔵 Draggable Circle Logo!")
